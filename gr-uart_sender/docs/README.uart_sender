This is the uart_sender-write-a-block package meant as a guide to building
out-of-tree packages. To use the uart_sender blocks, the Python namespaces
is in 'uart_sender', which is imported as:

    import uart_sender

See the Doxygen documentation for details about the blocks available
in this package. A quick listing of the details can be found in Python
after importing by using: 

    help(uart_sender)
